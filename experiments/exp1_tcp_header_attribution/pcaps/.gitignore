# Transformed copies of the large "L" captures are big; their SHA-256 hashes are
# recorded in ../out/transform_manifest.json. The small short-capture transforms
# are committed. Original PCAPs are never stored here (they stay in the frozen repo).
*L_T0.pcap
*L_T1.pcap
*L_T2.pcap
*L_T3.pcap
