# Compiler output trees. They are LARGE (each ~15 MB) and fully reproducible from the
# P4 plus the documented bf-p4c invocation, so only the small artefacts that are
# EVIDENCE -- the .bfa assembly and the resource logs -- are kept, under artifacts/.
p4/build_*/
p4/bx_*/
