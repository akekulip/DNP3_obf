# Overnight Autonomy Run — 20260723-2255

Append-only engineering log for the bounded autonomous run authorized by `autunomous.md`.
Governing rules: that file (authority), plus repo `CLAUDE.md` and project safety rules.
No secrets/passwords/keys/tokens in this log.

Format per entry: command · host · start/end · exit · output(relevant) · interpretation ·
next decision · restoration status.

---

## CHECKPOINT (Stage 0) — 2026-07-23 22:55 EDT

**Local control host:** gambit (this host), `/home/philip/Projects/DNP3`.
**Branch:** `overnight-autonomy-20260723-2255` (created off HEAD `e3a7d01`, on `research/caseA-ditto-queue`).
**HEAD at start:** `e3a7d01` (docs(shadow): correct dp8 conclusion — intermittent link, root cause unisolated).
**git status:** `dnp3_split_harness/split_server.py` modified (pre-existing, not mine); many untracked
`compile_defense1_telem*` build artifacts + `autunomous.md` + `dnp3_queue_microbench_snapshot.zip`
(pre-existing untracked, left alone per surgical-change discipline).

**Switch (decps@10.10.54.15):**
- bf_switchd running, conf = `/home/decps/queue_microbench/out/queue_microbench_abs.conf` (queue microbench).
- BF-SDE: `bf-sde-9.13.2`.

**Vision (decps@10.10.54.19):** host=vision
- eno1 = `192.168.10.1/24` (relay) ✅ accepted state
- eno2 = `10.10.54.166/24` + `10.10.54.19/24` (mgmt) — `.19` present+reachable ✅; `.166` is a
  pre-existing DHCP lease (NOT created this run) — left in place per "do not clean up working interfaces".
- enp59s0f0np0 (dp8 data NIC) = DOWN; driver i40e, fw `8.15 0x800096ca 20.0.17`, bus `0000:3b:00.0`.
- From Vision: ping Hulk `10.10.54.158` OK; ping SEL `192.168.10.7` OK.

**Hulk (decps@10.10.54.158):** host=hulk; enp59s0f0np0 (dp9 data NIC) = DOWN (switch ports not enabled).

**Reachability from gambit:** Vision .19 OK, Hulk .158 OK, switch .15 OK.

**Accepted dp8 conclusion (carried in, per autunomous.md):** dp8 previously achieved operational link
and received six clean frames after physical reseating, but failed to reproduce during the subsequent
GATE-1 attempt. Intermittent physical/link-layer fault; root cause unisolated pending a cold power cycle
and controlled substitution tests. NOT a false positive; NOT isolated to any single component.

**Vision reboot budget this run:** 1 graceful reboot (not yet used).

---

## STAGE 1 — Reconcile (read-only) — 2026-07-23 23:00 EDT — PASS

Verified present/valid (no network change):
- Switch: `dnp3_shadow/build_9132/dnp3_shadow_abs.conf` (1037 B), `queue_microbench/out/queue_microbench_abs.conf`
  (590 B), `dnp3_shadow_setup.py`, `fix_dp8.py`, `launch_shadow.sh`, `shadow_read_counters.py`, `lane_probe.py`.
- Vision `~/shadow_gate1/`: `dp8_inject.pcap` (55134 B, 606 frames), `shadow_raw_replay.py`.
- Hulk `~/shadow_gate1/`: `dp9_inject.pcap` (89856 B, 605 frames), `shadow_raw_replay.py`.
- Repo shadow scripts: `shadow_pcap_split.py`, `shadow_raw_replay.py`, `shadow_read_counters.py`,
  `shadow_refmodel.py` (imports OK, `classify` present), `verify_shadow_run.py`, `replay_shadow.py`,
  `test_shadow_negative.py`.
- Normative specs: `research/END_TO_END_MISSION_CHARTER.md`, `research/END_TO_END_IMPLEMENTATION_PLAN.md`
  (Phase order + gap matrix: Phase 1 shadow done offline / partial silicon; Phase 2 txn-core = add
  generation freshness to the EXISTING `{cip,sip,cport}` CRC16 flow hash in `dcrn_defense1.p4`; Defense 1
  = 12/12 stages FULL; Defense 2 = 10/12, mis-calibrated recirc/qid; size regen not implemented; joint =
  platform split, single-program infeasible).
- Frozen defense P4: `dcrn_defense1.p4`, `dcrn_defense2.p4` (+ `_telem` variants).
- Committed replay source: `research/physical_sel751/clrt_300poll_20260723T152242/evidence/clrt_300poll_20260723T152242.pcap` (144966 B).
- Offline verifier deps: gambit sys-python scapy 2.4.3, research-python scapy 2.7.0 — comparison tooling runnable.
- SSH: Vision `.19` OK, Hulk `.158` OK, switch `.15` OK. From Vision: Hulk OK, SEL OK.

Interpretation: repo + testbed consistent with the accepted state; nothing missing to attempt GATE-1 or
to proceed offline. **Network left unchanged.** Next: Stage 2 bounded dp8 recovery.

---

## STAGE 2 — Bounded dp8 recovery — 2026-07-23 23:05 EDT

**Baseline preserved:** microbench `$PORT` empty (no configured ports, dp8 absent) → saved
`/home/decps/dnp3_shadow/mb_port_baseline_20260723-2255.json` on switch. Restore = remove dp8 (or cold
reload microbench conf).

**Mapping re-verified (once):** dp8 = Vision `enp59s0f0np0`, MAC `3c:fd:fe:cc:5d:c0`, bus `0000:3b:00.0`,
driver i40e fw `8.15`, lane 15/0 — matches the documented map.

**Attempt 1 (no reboot):** `lane_probe add 8 queue_microbench` → 25 G / RS-FEC / PM_AN_DEFAULT / LPBK_NONE
/ ENABLE applied. After 20 s: switch dp8 `$PORT_UP=false`, `$PORT_ENABLE=true`, 0 frames rx, 0 errors;
Vision carrier=0, Speed Unknown, Link detected: no. **dp8 did not link** (consistent with the intermittent
fault). dp8 left ENABLED on the switch so a fresh negotiation can form when Vision re-inits its NIC.

**Reboot risk assessed (read-only):** Vision has TWO autoconnect eno1 profiles — `eno1-dhcp` (manual,
`10.10.54.19/24`+`192.168.10.1/24`, currently active) and `eno1-static` (manual, `10.10.54.71/24`, no
`.19`). Mgmt `.19` currently lives on eno2 as a runtime-only addr (not in eno2's DHCP profile → will NOT
survive reboot). After reboot, `.19` returns only if `eno1-dhcp` reactivates (likely, it is the active
profile). Per doc I must NOT modify NM to force this. If mgmt does not return → red-line #2 (stop the
hardware line; continue offline on gambit). Proceeding with the single authorized graceful reboot.

**Attempt 2 (the one authorized graceful reboot):** `sudo systemctl reboot` on Vision at 23:02:41; Vision
DOWN by 23:03:00, SSH back at 23:06:40 (~3.5 min, within recovery period). **Mgmt returned — no red-line.**
Post-reboot addressing (Vision's own persistent `eno1-dhcp` profile applied): **eno1 = `10.10.54.19/24` +
`192.168.10.1/24`; eno2 = `10.10.54.166/24` (DHCP)**. From Vision: SEL `192.168.10.7` OK, Hulk OK. dp8
switch port was left enabled through the reboot → Vision came up fresh → **dp8 still DOWN** (carrier=0,
Link no).

**Attempt 3 (config re-apply, the one authorized repeat):** removed + re-added dp8 on the switch at the
documented 25 G / RS-FEC / PM_AN_DEFAULT config against the freshly-booted Vision NIC; after 30 s **dp8
`$PORT_UP=false`, 0 frames, Vision carrier=0.**

**STAGE 2 RESULT — dp8 NOT recovered.** Bounded recovery exhausted (1 enable + 1 graceful reboot + 1
re-apply), NO forbidden actions used (no FEC permutations, no driver reload, no PCI rescan, no NM/IP
edits). Consistent with the accepted conclusion: intermittent physical/link-layer fault whose real
remedy (a **cold** power cycle + controlled substitution) needs on-site physical action — a warm reboot
did not clear it. **Restored:** dp8 removed from `$PORT` → microbench `configured_ports=[]` (matches the
23:05 baseline), bf_switchd still on `queue_microbench_abs.conf`. **Decision: proceed to OFFLINE FALLBACK**
(doc: "do not waste the entire night retrying the physical link"). GATE-1 hardware (Stage 3) and the SEL
baseline (Stage 4) stay BLOCKED and are NOT marked complete.

**Network deviation noted (not corrected — per the no-NM/IP-edit rule):** the authorized reboot left
mgmt `10.10.54.19` on **eno1** (with relay `192.168.10.1`), not on eno2 as the doc's ideal final state
lists. Functional requirements are met — Vision reachable at `.19`, relay `.1` present and SEL reachable —
so this is left as the reboot produced it; I did not touch NetworkManager/addressing to reshape it.

---

## OFFLINE FALLBACK — 2026-07-23 23:10 EDT

dp8 physically blocked → productive offline work on gambit (no hardware GATE-1). Clearly labeled
provisional: nothing here is hardware- or relay-validated. Plan: (1) re-establish the Phase-1 shadow
OFFLINE regression baseline (refmodel replay + negative tests) as fresh evidence; (2) assess Phase-2
transaction-core spec readiness + local compile path; (3) if the frozen design is concrete enough,
implement generation-freshness on a COPY with TDD + local compile — never editing the frozen
`dcrn_defense1.p4`.

### OFFLINE-1 — Phase-1 shadow regression — 23:12 EDT — PASS
`$RESEARCH_PYTHON` (scapy 2.7): `replay_shadow.py` PASS (n_ip_tcp=1211; DNP3_READ=300, DNP3_RESPONSE=300,
PURE_ACK=605, 300 separate-ACK→response triples, no zero-ACK-as-DNP3, no link-frame-as-app, 0 retransmit,
byte/size/order identity by construction). `test_shadow_negative.py` 14/14 OK. Fresh offline evidence
that the Phase-1 classifier logic is intact. (Hardware GATE-1 still blocked — not re-claimed.)

### OFFLINE-2 — Phase-2 generation-safe transaction core (offline) — 23:15–23:45 EDT — PASS
New `research/tofino_dcrn_feasibility/p4/ack_delay/txncore/` (plan §8). Faithful Python mirror of the
frozen `dcrn_defense1.p4` transaction core + the generation-freshness guard the frozen file deferred
(`reg_gen` dropped, `bridge.gen`≡0). TDD: tests written first (failed: ModuleNotFoundError), then model.
- `tests/test_txncore.py`: **22/22 PASS** — correlation, direction, retransmit, dup/stale ACK,
  resp-before-ACK (combined bypass), FIN/RST abort, timeout/fail-open, 2nd request re-arm, seq-32b wrap,
  gen rollover (mod 256), hash-collision → STALE_DISCARD (not misreleased), pass-through, no-stale-state.
- `replay_txncore.py` over the committed 300-poll pcap: **PASS** — ARM=300, ACK_HELD=300, RESP_HELD=300,
  ACK_RELEASED=300, RESP_RELEASED=300, STALE_DISCARD=0, residue=0, single flow. (Driver corrected mid-run
  to apply the real DNP3 application + function-code gate — short link frames must not arm; only func-1
  READs do — reusing `shadow_refmodel.classify`. The model was faithful; the driver's featurization was
  the bug, found and fixed via the replay.)
Report: `txncore/TXNCORE_PHASE2_REPORT.md`. Interpretation: Phase-2 LOGIC (the part "used by 3/4") is
offline-validated. Remaining Phase-2 item "fits a variant" (P4 compile-fit of the gen register on a
Defense-1 copy) is Phase-3-adjacent and gated by the 12/12 stage budget — attempted next, bounded.
**Nothing here is hardware- or relay-validated.**
