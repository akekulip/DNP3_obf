# bf-p4c build trees — regenerate with:
#   PATH=/home/philip/bf-sde-9.13.1/install/bin:$PATH \
#     bf-p4c --target tofino --arch tna -g -o out p13_size_do8.p4
# The compile LOGS are the evidence and ARE tracked; only the binary/IR trees are ignored.
out/
**/out/
__pycache__/
*.pyc
