# Socket-level ACK-separation experiment (Phase 2A)

Generated by `ack_separation_probe.py` (plan section 5A). Question: can DELAYING the application write() induce the host TCP stack to emit a **pure TCP ACK** before the DNP3 response -- i.e. turn an ACK-bearing DNP3 response into `request -> pure TCP ACK -> DNP3 response` -- with NO packet forging? Forging a TCP ACK is out of scope for this phase.

## Run metadata

| field | value |
| --- | --- |
| mode | loopback |
| capture method | **none** (capture tool present but this user lacks capture privilege (dumpcap permission denied); needs CAP_NET_RAW or root) |
| connection | 127.0.0.1 -> 127.0.0.1:20051 |
| capture interface | lo |
| delays (ms) | 0,1,2,5,10,20,50 |
| reps per (config,delay) | 20 |
| response size (B) | 2407 |
| socket-option configs | baseline, server_nodelay_on, server_nodelay_off, client_quickack_on |
| TCP_QUICKACK available | yes |
| python | 3.12.13 |
| host | gambit |

## Result matrix (aggregate per config x delay)

| config | server NODELAY | client QUICKACK | delay (ms) | reps | req->resp mean (ms) | req->resp max (ms) | resp OK | bytes OK | resets | pure ACK (yes/no/unk) | pure ACK frac | req->ACK mean (ms) | ACK->resp mean (ms) | retrans sum |
| --- | --- | --- | ---: | ---: | ---: | ---: | ---: | :---: | ---: | :---: | ---: | ---: | ---: | ---: |
| baseline | default | default | 0 | 20 | 0.02 | 0.028 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| baseline | default | default | 1 | 20 | 1.104 | 1.157 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| baseline | default | default | 2 | 20 | 2.102 | 2.117 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| baseline | default | default | 5 | 20 | 5.311 | 6.065 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| baseline | default | default | 10 | 20 | 10.321 | 10.821 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| baseline | default | default | 20 | 20 | 20.462 | 20.811 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| baseline | default | default | 50 | 20 | 50.467 | 50.797 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_on | on | default | 0 | 20 | 0.038 | 0.049 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_on | on | default | 1 | 20 | 1.099 | 1.12 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_on | on | default | 2 | 20 | 2.216 | 3.009 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_on | on | default | 5 | 20 | 5.334 | 5.965 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_on | on | default | 10 | 20 | 10.384 | 10.798 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_on | on | default | 20 | 20 | 20.489 | 20.75 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_on | on | default | 50 | 20 | 50.409 | 50.832 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_off | off | default | 0 | 20 | 0.037 | 0.052 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_off | off | default | 1 | 20 | 1.107 | 1.133 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_off | off | default | 2 | 20 | 2.13 | 2.52 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_off | off | default | 5 | 20 | 5.122 | 5.147 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_off | off | default | 10 | 20 | 10.46 | 10.784 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_off | off | default | 20 | 20 | 20.461 | 20.854 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| server_nodelay_off | off | default | 50 | 20 | 50.445 | 50.847 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| client_quickack_on | default | on | 0 | 20 | 0.035 | 0.063 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| client_quickack_on | default | on | 1 | 20 | 1.132 | 1.638 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| client_quickack_on | default | on | 2 | 20 | 2.102 | 2.107 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| client_quickack_on | default | on | 5 | 20 | 5.13 | 5.144 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| client_quickack_on | default | on | 10 | 20 | 10.193 | 10.276 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| client_quickack_on | default | on | 20 | 20 | 20.255 | 20.647 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |
| client_quickack_on | default | on | 50 | 20 | 50.489 | 50.872 | 20 | yes | 0 | 0/0/20 | n/a | n/a | n/a | n/a |

## Interpretation

- **Pure-ACK EMISSION could NOT be determined on this run.** Detecting a pure TCP ACK requires a packet capture, and no unprivileged capture was available here (capture tool present but this user lacks capture privilege (dumpcap permission denied); needs CAP_NET_RAW or root). The tool did NOT guess yes/no: every `pure ACK` cell is reported as unknown (`unk`), and `req->ACK` / `ACK->resp` are `n/a`.
- What WAS measured on this run: `request_to_response_ms` (client-side round trip via `time.monotonic_ns()`), DNP3-response success (`resp OK`), byte-size correctness (`bytes OK`), and resets, per (config, delay, rep). The round trip tracks the applied application-write delay, confirming the delay mechanism works; it says nothing about ACK piggybacking.
- To fill the ACK columns, re-run with a privileged capture (tcpdump/tshark with CAP_NET_RAW or root), ideally on the two-host rig below.

## Socket-option findings (one factor at a time)

- The sweep varies exactly ONE factor per config vs `baseline`: server-side `TCP_NODELAY` on, `TCP_NODELAY` off, then client-side `TCP_QUICKACK` on (where the option exists). The options actually set are recorded on every row (`server_nodelay`, `client_quickack`).
- Because pure-ACK emission is a *capture-only* observation, the effect of these options on ACK separation is NOT resolved on this run -- only their (minor) effect on `request_to_response_ms` is visible here.
- Endpoint note: the pure ACK of interest is the **server** ACKing the client's request, so the **server's** delayed-ACK / QUICKACK behaviour is what governs it. This sweep sets `TCP_QUICKACK` on the CLIENT (per the plan), which governs the client's ACK of the server's response instead. A follow-up that toggles QUICKACK / the delayed-ACK timer on the SERVER is the direct knob for the request->pure-ACK behaviour and is a natural next one-factor extension.

## Offload and host-capture caveats

- **TSO/GSO/GRO and checksum offload** can make a host capture show segmentation and sizes that differ from the actual on-wire packets: with GRO/LRO the capture may coalesce several wire segments into one large record, and with TSO/GSO the capture may show one large record that the NIC later splits. This can hide or merge a pure ACK relative to the response, so trust NIC-level or peer-side captures over a same-host capture when segmentation matters.
- **A host-side capture is not the wire.** It taps between the application and the NIC, so its timestamps include host queueing and are not the exact on-wire times (plan section 12: "a host-side capture does not always represent exact wire timing").
- **Loopback (`--loopback`) is doubly unrepresentative.** The `lo` path has ~0 ms delay and no loss, the delayed-ACK dynamics differ from a real NIC/wire, and loopback offload behaviour is its own special case. Loopback is a smoke test of the mechanism and the tooling only.

## Honest caveats (plan section 12 -- strict research claims)

- **Socket delay does NOT always force a separate ACK.** Whether a delayed write produces a pure ACK depends on the receiving kernel's delayed-ACK timer, quickack state, the peer's window/behaviour, and offload -- it is a host/kernel behaviour, not a protocol guarantee. Any positive result is "measured on this device, trace, host, and configuration", not a general claim.
- **Loopback != wire.** Do not generalise any loopback observation to the rig or to a real DNP3 device.
- Not all DNP3 devices use the same TCP ACK behaviour; results here do not transfer across devices or stacks without re-measurement.

## Two-host rig command (authoritative pure-ACK detection)

Run on the real rig (master **Vision 10.10.54.19** <-> outstation host **Hulk 10.10.54.158**) with a PRIVILEGED capture so the pure ACK is actually observed on the wire. Capture at both endpoints if possible (plan 5A):

```bash
# On Hulk (outstation host 10.10.54.158) -- run the delay server:
python3 ack_separation_probe.py --server --bind-ip 0.0.0.0 --port 20051 --resp-size 2407
#   (optional both-endpoint cross-check, in a second Hulk shell):
#   sudo tcpdump -i <egress-iface> -w hulk_ack_sep.pcap 'tcp port 20051'

# On Vision (master host 10.10.54.19) -- run the measuring client UNDER sudo
# so its tshark capture path activates and pure-ACK detection works:
sudo python3 ack_separation_probe.py --client --connect-host 10.10.54.158 --port 20051 \
    --iface <egress-iface> --delays 0,1,2,5,10,20,50 --reps 20 --resp-size 2407
```

Under `sudo` on Vision the client's `tshark` capture path activates automatically; the pcap is analysed to fill `pure_ack_emitted`, `request_to_ack_ms`, `ack_to_response_ms`, `retransmissions`, and per-rep stability. Without capture privilege the run still records timing + DNP3 success and marks pure-ACK detection as undetermined.
