# Defense 4 — risk register

**2026-08-04. Each risk: cause, impact, likelihood, evidence, mitigation, kill criterion, owner.
Severity ranks the product of impact and likelihood. Kill criterion = the observation that would force
abandoning or re-scoping that element.**

| # | risk | cause | impact | likelihood | evidence | mitigation | kill criterion | owner |
|---|---|---|---|---|---|---|---|---|
| R1 | **Strong claim overstated** — "READ and SBO indistinguishable" claimed on plaintext | plaintext function code = O(1) classifier; TF1 can't encrypt the residual | paper rejected / retracted; scientific integrity | **certain if unmanaged** | VERIFIED (func code visible in pcap; residual never in PHV) | scope base claim to Profile A difference-reduction; gate the strong claim on an explicit external MACsec assumption, stated in the threat model | reviewer shows the function code distinguishes the shaped streams (it will) → strong claim dropped, not the work | lead / security |
| R2 | **Combined ingress core exceeds 12 stages** | D3 already 10/10 dependency-bound; +D2 deadline +SBO key | no single-binary Defense 4; must split into 2-pass or drop a mode | **~50% (coin-flip)** | VERIFIED D3 10/CP10; bounded 11–12 | run MB-1 (E1) BEFORE any P4 build; fallbacks ready (drop a mode / egress-bridge the SBO key / 2-pass) | MB-1 > 12 with all fallbacks exhausted → the unified single-binary goal is abandoned; ship separate configs | P4 eng |
| R3 | **No real SBO size corpus** blocks the entire size/READ-vs-SBO half | DIRECT_OPERATE ≠ SELECT→OPERATE; physical OPERATE prohibited | size claims + tasks T1/T3 unevaluable | high until E2 done | VERIFIED emulator SBO exists but single fixed count | E2: run `run_multicrob_sweep.py` at N∈{1,2,4,8,16} on the emulator — no relay | emulator cannot produce the needed CROB-count variation → size envelope undefined, ship timing-only | DNP3 |
| R4 | **k=1 device** — no anonymity claim reachable | one physical relay | any device-fingerprinting/anonymity claim is unsupported | certain | VERIFIED (single SEL-751) | scope to feature-removal, not anonymity; synthetic-population falsifier (E3) for device-independence; acquire a 2nd separate-ACK device for any positive claim | Philip cannot obtain a 2nd device → anonymity permanently out of scope | Philip / lead |
| R5 | **SBO select-timeout violated** by reverse-path holds → control silently fails | a SELECT-response hold delays the OPERATE, eating the select-arm budget | an issued control silently does not execute — worse than a slow poll | medium (depends on relay setting) | VERIFIED coupling; opendnp3 default 10 s; SEL setting BLOCKED | phase-specific `Θ`; add `2·(N+1)·T + turnaround + M < selectTimeout` to `parameter_policy`; read the SEL setting before any live SELECT | the relay's selectTimeout is set below the required hold at any usable (T,N) → no live SBO shaping on that relay | DNP3 / control |
| R6 | **Observer also taps the native endpoint ports** → defense void | threat model assumes observer only on the protected segment | entire defense pointless if violated | deployment-dependent | INFERRED (topology) | pin the observer location in the threat model; the protected link must be the ONLY exposed segment | a realistic deployment exposes the native ports → the one-switch model is inapplicable; needs true two-edge deployment | security / topology |
| R7 | **Internal recirc used for the "protected link"** → observer-invisible, model void | using dp8 internal recirc instead of an external cable | the shaped representation is never on an observable link; results meaningless | medium (easy mistake) | VERIFIED (recirc invisible) | the protected link MUST be an external front-panel loopback, tapped on the fiber; assert in MB-6 | capture on the loop shows internal-only path → re-cable before any leakage measurement | topology / testbed |
| R8 | **Decoy CROB actuates the relay** | a "valid-but-unwired" index drives a SELOGIC remote bit, not just a dead output | unauthorized control on a protection relay | medium if attempted without V1 | VERIFIED hazard class; SEL wiring BLOCKED | decoy CROBs master-side only + V1 inertness proven on the real relay before any OPERATE; SELECT observe-only until then | V1 cannot find a provably inert index on the SEL-751 → decoy-CROB padding dropped, use master-side request padding only | power-systems / Philip |
| R9 | **Fabricated DNP3 reaches an endpoint** (filler, CONFIRM, time-sync) | a decoder leak or an inner-object filler | SOE deletion / relay clock change / data-model corruption | low if design honored | VERIFIED (opendnp3 IIN + CONFIRM effects) | strictly byte-preserving inner DNP3; filler outer-encapsulated only; decoder proven to strip all filler (MB-6) | any filler frame observed on dp9/dp64 → halt, the decoder is unsafe | DNP3 / P4 |
| R10 | **Retransmission/duplicate → double actuation** | non-idempotent handling of a duplicate OPERATE | a control executed twice | low-medium | PROPOSED (corner cases #7/#8) | idempotent keying on (flow,link,app_seq,seq); a duplicate binds to the existing entry, never a new one | a duplicate OPERATE binds twice in test → redesign the key before any live SBO | DNP3 / P4 |
| R11 | **Inter-arrival reported as a device leak** | the pydnp3 test master is response-paced | a false leakage claim in the paper | medium if unmanaged | VERIFIED (E0: tracks D, harness artifact) | exclude inter-arrival from discriminative features; report with/without; note the wall-clock-poller caveat | — (managed by analysis discipline) | evaluation |
| R12 | **Cellization assumed for large READs** | a multi-fragment READ larger than one frame | overclaimed capability; v1 breaks on big responses | low (if envelope declared) | INFERRED (arch §6 defers it) | declare a bounded READ envelope; fail open outside it; keep cellization future work | a required workload exceeds one frame per unit → cellization becomes a hard dependency, re-scope | size |
| R13 | **Resource total fabricated** before the combined compile | pressure to quote a number | wrong budget drives a bad design | low (flagged) | VERIFIED discipline | never quote a combined ingress total until MB-1; give bounded budgets + the compile that replaces them | — | P4 / lead |

## Added after adversarial review

| # | risk | cause | impact | likelihood | evidence | mitigation | kill criterion | owner |
|---|---|---|---|---|---|---|---|---|
| R14 | **Two-edge deployment invalidates the single-switch result** | the one-switch loopback shares one generation/slot register and ONE pktgen epoch counter; two real switches share neither, and cross-switch grid epoch sync (arch RQ9) is unanswered | the central grid result (release on a common `t_k`) and the byte-identity/filler-strip safety property do not transfer to a real two-edge deployment | high for any deployment claim | VERIFIED (single-box shares registers; RQ9 open) | scope every two-edge claim to "single-box, observer-on-one-cable"; a real deployment needs two switches + a data-plane epoch-sync mechanism (future work) | a two-switch epoch-sync mechanism cannot be built in the data plane without a fast-path controller → the two-edge / distance-link deployment is infeasible on TF1, keep it single-box lab-only | topology / P4 |
| R15 | **Motivation inverted + platform undefended** | the study framed shaping as needing crypto, when crypto (802.1AE) is porous to size/timing so shaping COMPLEMENTS crypto; and the "Tofino-1 only" choice is undefended vs a 5-pps host-edge encrypt-and-shape box (IPsec-TFC / NetShaper) | reviewer rejects on "why a Tofino at 5 pps" and on backwards motivation | high if unmanaged | VERIFIED (802.1AE preserves plaintext length; 5-pps line-rate advantage moot) | reframe: encryption is porous → shaping complements it; explicitly defend the platform (or accept the design ports off-Tofino) | no defensible reason to prefer a Tofino over a host-edge box at ICS rates → the platform premise is dropped and the contribution is protocol+safety, not in-network-at-line-rate | lead |
| R16 | **Benchmarked against the wrong baseline** | the study compares to its own Defense 3, not to Ditto/NetShaper (the field's baseline); schedule-anchored release IS Ditto's principle | novelty overstated; the conceptual "contribution" is Ditto's existing property re-derived | high | VERIFIED (review, Ditto NDSS'22) | reframe the deltas explicitly against Ditto/NetShaper: the `a/T` bidirectional-coupling lemma, the SBO select-timeout admissibility bound, the OT safety envelope, and the same-hardware event-vs-schedule comparison | none of those deltas survives as novel against Ditto → the work is a straight application/systematization, venue TDSC/ICS-track only | lead |

## Highest-severity, act-first

- **R2** (ingress stage budget) and **R1** (claim scope) are the two that decide whether Defense 4 as
  specified is buildable and publishable. R2 is resolved by MB-1 (offline, do first); R1 is resolved by
  writing the claim boundary into the threat model now (done in the spec).
- **R5 / R8 / R9 / R10** are the safety cluster — all gated on a SEL-751 device-profile read and on the
  strict no-fabricated-DNP3 / no-live-OPERATE rules. None may be relaxed without explicit authorization.
