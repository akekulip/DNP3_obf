Two-pipe BOR split — bf-p4c 9.13.1 (~/bf-sde-9.13.1), --target tofino --arch tna -g. TF1: 12 ingress stages max.
Stage counts are the FINAL (last-round) 'Number of stages for ingress' in pipe/logs/table_summary.log.

REFERENCE
  frozen RRC kernel (defense4_rrc_kernel.p4)                         ingress=12 egress=3  FITS   (INITIAL 13 -> settles 12)

PIPE 1  (defense4_twopipe_pipe1_probe.p4, BOR OPERATE hold/release core, standalone)
  (no flags)                                                          ingress=6 egress=0  FITS

PIPE 0  (defense4_twopipe_pipe0_probe.p4 = frozen RRC + T0-admission + cross-pipe route)
  FINAL RECIPE  -DTWO_PIPE_PIPE0 -DBOR_NO_TOPJ -DPIPE0_ARM_FOLD       ingress=12 egress=3  FITS   *** the faithful build ***

  ---- isolation / diagnostics ----
  no fold           -DTWO_PIPE_PIPE0 -DBOR_NO_TOPJ                     ingress=13 NO-FIT   (the naive split: OVER by 1)
  no fold +SR1/2/3  -DTWO_PIPE_PIPE0 -DBOR_NO_TOPJ -DBOR_NO_CODEBOOK
                    -DSR1_TM_DISPATCH -DSR2_FOLD_PARAMS -DSR3_CP_TS_GATE ingress=13 NO-FIT   (consolidations do NOT recover it)
  drop T0-anchor    -DTWO_PIPE_PIPE0 -DBOR_NO_TOPJ -DBOR_NO_T0ANCHOR
                    -DBOR_NO_CODEBOOK -DSR1_TM_DISPATCH -DSR2_FOLD_PARAMS ingress=12 FITS   (DIAGNOSTIC: T0-anchor IS the +1; NOT faithful)
  fold only         -DTWO_PIPE_PIPE0 -DBOR_NO_TOPJ -DPIPE0_ARM_FOLD    ingress=12 FITS   (arm-fold alone recovers the stage)

CONCLUSION
  The cross-pipe route is free (drop-T0-anchor build already fits at 12). The isolated +1 stage
  in the naive split is the anti-subtraction T0-ANCHOR's write-after-write on dl_val. Folding that
  arm into the decode action (PIPE0_ARM_FOLD, the base-RRC dec_ack_arm shape) removes it: pipe 0
  fits 12/3 WITH the faithful T0-anchor, pipe 1 fits 6/0. Both <= 12.  VERDICT: FEASIBLE.

CONTROL — is the split actually NECESSARY, or does the arm-fold rescue ONE pipe?
  one-pipe RRC+BOR (OPERATE HOLD present) + arm-fold + SR1/SR2  ingress=13  NO-FIT
  => Even WITH the fold, the one-pipe build with the OPERATE hold is 13. The fold removes the
     T0-anchor stage, but the OPERATE hold/release core (qid2/qid3/reg_topj/OP_RELEASE) remains a
     separate +1. The two-pipe split moves that core to pipe 1 (6 stages), which is what makes
     pipe 0 land at 12. The split is necessary AND sufficient.
