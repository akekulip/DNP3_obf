SEL-751 read-only baseline — 2026-08-12
========================================
Method : read-only DNP3 Class-0 poll (relay_output_read.py), function 0x01 only, no controls.
Path   : Vision 192.168.10.1 (enp59s0f0np0, dp9/15/1) -> Tofino-1 10.10.54.81 (defense4_caseA, OFF/transparent)
         -> dp64/33/0 "E1/33" -> unmanaged switch -> SEL-751 192.168.10.7:20000
Link   : master=1, outstation=0.  RTT ~0.35 ms.  IIN = 84 00 (DEVICE_RESTART + class-2 pending, no errors).

Binary Output Status (G10 var2), 32 points, index 0..31:
    ALL OPEN/OFF (bit=0), all online.

Binary Input (G1 var2), 16 points, index 0..15:
    point 0 = CLOSED/ON (bit=1);  points 1..15 = OPEN/OFF;  all online.

Point convention for the two-CROB proof (Philip, 2026-08-12; valid because all outputs are open):
    OUT_REAL  = EVEN output points (0, 2, 4, ...)
    OUT_DECOY = ODD  output points (1, 3, 5, ...)

SBO SELECT baseline (relay_select_baseline.py, SELECT-only / NON-ACTUATING):
    SELECT G12V1 CROB PULSE_ON to output point 0 (even/OUT_REAL): status 0x00 (SUCCESS, armed).
    SELECT echo = 35 B on-wire, single G12V1 CROB (group 0x0C).
    echo hex: c28184000c011701000101c80000000000000000
    Point 0 read OPEN before AND after -> SELECT did not actuate (only OPERATE would).
    Native size compare: READ(G1)=40B vs SBO-select-echo=35B; structure G1/G30 vs G12 CROB.
    NOTE: point 0 accepted the arm -> it is a live controllable output.

Full native SBO (relay_sbo_operate.py, AUTHORIZED single actuation 2026-08-12: "operate it, point 0 is safe"):
    SELECT  echo 35B status 0x00 : c28184000c011701000101c80000000000000000
    OPERATE echo 35B status 0x00 : c38184000c011701000101c80000000000000000   (both G12V1 CROB, group 0x0C)
    Point 0: OPEN before -> PULSE_ON 200 ms -> OPEN after (self-restored; no LATCH_OFF needed).
    NATIVE SBO transaction = two 35 B G12 CROB echoes + two-phase request (SELECT 0x03, OPERATE 0x04).

2-CROB SBO (real+decoy) baseline (relay_sbo_2crob.py, SELECT-only / NON-ACTUATING):
    SELECT one G12V1 object, count 2: real CROB -> even point 0, decoy CROB -> odd point 1.
    both statuses 0x00 (armed); select echo = 49 B on-wire (two G12V1 CROBs).
    echo hex: c28184000c011702000101c80000000000000000010101c80000000000000000
    Points 0 and 1 both OPEN before AND after -> non-actuating.

Proof O_count comparison (MEASURED on the real relay):
    READ(G1)     = 40 B         SBO 1-CROB echo = 35 B
    READ(Class0) = 134 B        SBO 2-CROB echo = 49 B   (real+decoy)
    -> the decoy CROB lifts the SBO echo to 49 B, just above the 40 B READ; padding the READ 40->49 B
       matches O_count. Both READ and SBO responses are a single TCP packet (segmentation already 1 pkt),
       so PAD-only normalizes O_count + segmentation; SPLIT not required for these small responses.
    Residual (O2): structure G1/G30 vs G12 CROB, and request 1x0x01 vs 0x03+0x04 -> needs the decoy
       objects/requests (the O2 template) to also collapse; O_count/O1 is closed by pad alone.

Provenance: reproduce with  ssh decps@10.10.54.19 python3 - < relay_output_read.py
(requires the Tofino forwarding Vision<->relay: dp9<->dp64 up under a loaded program).
No control command was ever transmitted.
