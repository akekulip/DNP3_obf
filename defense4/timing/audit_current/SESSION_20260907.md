# Session record, 2026-09-07

What was verified, what was corrected, what was left alone, and what was not run. The gate
table is the short version.

---

## 1. Gate table

| # | gate | result | evidence |
|---|---|---|---|
| 1 | repository state recorded: status, remote, branches, worktrees, log, tags | **PASS** | `BRANCH_MAP.md`, verified 2026-09-07 |
| 2 | the four spec-named repositories are one repository | **PASS** | `origin` of `/home/philip/Projects/DNP3` is `github.com/akekulip/DNP3_obf`; `--git-common-dir` is `.git`; the other three paths do not exist |
| 3 | five bundles verify with complete history | **PASS** | `git bundle verify` on each; none overwritten |
| 4 | the required pre-cleanup bundle already existed and was preserved | **PASS** | `DNP3_obf-before-timing-cleanup-20260824.bundle`, 112,292,049 bytes, 2026-08-24 |
| 5 | archive tag and cleanup branch already existed; not recreated | **PASS** | tag at `8a6896e`, branch at `2ea2daf` |
| 6 | `git fetch --all --prune` with no pull, merge or rebase | **PASS** | no remote ref moved |
| 7 | `main` tip matches the spec's `883d8cd…` | **PASS** | exact match |
| 8 | the spec's evidence tip `8a6896e` is an ancestor of the local branch, not a rewrite | **PASS** | ancestor confirmed; 10 commits ahead |
| 9 | `defense4/` identical between `8a6896e` and the local tip | **PASS** | `git diff … -- defense4/` is empty |
| 10 | frozen P4 source hash | **PASS** | `7ce30494…` recomputed |
| 11 | four frozen harness files byte-identical to their `8a6896e` versions | **PASS** | all four, at both paths that commit carries |
| 12 | `E_FINAL` twin identity | **PASS** | both paths resolve to tree `1d1a5f3c…` at the archive tag |
| 13 | 22 dataset manifests, 268 entries | **PASS** | 0 problems |
| 14 | sweep manifest, 42 entries | **PASS** | 0 problems |
| 15 | 132 captures validated against every structural expectation | **PASS** | 0 problems |
| 16 | frozen table compared row by row, 63,360 rows, tolerance 1e-6 ms | **PASS** | 0 problems |
| 17 | reproduction test suite | **PASS** | 131 passed |
| 18 | publication gate against every published artefact | **PASS** | 0 problems |
| 19 | pipeline determinism across two independent runs | **PASS** | 14 of 14 artefacts byte-identical; only `environment.json` differs, and it records the commit |
| 20 | 18 published interval statistics re-derived by an independent parser | **PASS** | 18 of 18 agree; the one 0.4 µs gap is an even-sample median convention, explained |
| 21 | sweep transaction count reconciled | **PASS** | 5,860 from 19 points once the two restore runs are excluded |
| 22 | application log outcomes | **PASS** | 63,360 rows, all valid, all control statuses SUCCESS, 0 timeouts |
| 23 | TCP retransmissions across all 132 captures, both arms | **PASS** | 0, by two independent tools |
| 24 | select validity never violated | **PASS** | 2,640 of 2,640 obfuscated OPERATE SUCCESS, 0 `NO_SELECT` |
| 25 | corrected harness offline tests | **PASS** | 42 passed on Python 3.8 and 3.13 |
| 26 | the harness suite is not vacuous | **PASS** | six deliberate regressions injected, all six caught |
| 27 | frozen control-plane chain imports offline with no SDE | **PASS** | chain imports; all six modes exposed |
| 28 | two model figures render and pass the style gate | **PASS** | minimum 8 pt enforced on emit; both rendered outputs inspected and label collisions fixed |
| 29 | per-capture manifest built for campaign_v1 | **PASS** | 132 rows, 73 columns, CSV and JSON, no field inferred from a filename |
| 30 | manuscript gate on the flattened draft | **PASS** | `RESULT: PASS (no hard-check failures)`; warnings unchanged and pre-existing |
| 31 | no file deleted, moved or renamed | **PASS** | `git diff --name-status --find-renames` shows only A and M |
| 32 | frozen `final_read_sbo` configuration readback | **FAIL**, historical and frozen | `readbacks/hw_config_readback.txt` ends `RESULT: FAIL (n_fail=1 n_warn=0)` while showing no failing row; the failing assertion cannot be identified and the file is not edited |
| 33 | the master's realized TCP retransmission timeout | **UNRESOLVED** | kernel version and sysctls were never archived |
| 34 | the relay's select-validity window | **UNRESOLVED** | the setting was never read back |
| 35 | whether the relay retransmitted a held response | **UNRESOLVED** | the switch suppresses a position-matched duplicate, so it cannot be excluded master-facing |
| 36 | the switch's own release instants `e_a`, `e_r` | **UNRESOLVED** | never captured; the program latches them but the preserved control plane reads only four of six timestamp registers |
| 37 | realized per-transaction `J`, relay-facing release, exactly-once delivery | **UNRESOLVED** | no relay-facing tap exists |
| 38 | attribution of the 0.78 ms excess over the configured offset | **UNRESOLVED** | measured, but not attributable without `e_a` |
| 39 | configuration readback for campaign_v1 | **PARTIAL** | one summary line per block for 126 of 132 captures; s01's six have none; no full transcript anywhere |
| 40 | physical operation time | **NOT MEASURED** | no physical event was defined or instrumented, and none is proposed |
| 41 | any hardware operation, program load, or configuration change | **NOT RUN** | not authorized; nothing attempted |
| 42 | any new capture or campaign | **NOT RUN** | not authorized |
| 43 | the rerun plan itself | **NOT RUN** | written, awaiting a decision |
| 44 | the paired switch-side readback of §3 of the rerun plan | **NOT RUN** | needs the two-entry control-plane addition, then hardware |
| 45 | manuscript section edits | **NOT RUN** | held in the proposed patch |
| 46 | Inkscape re-export of the two schematics | **NOT RUN** | would change manuscript figures and `main.pdf`; held in the proposed patch |
| 47 | any push, merge, branch or tag change | **NOT RUN** | six local commits, nothing pushed |

No gate was recorded PASS on the strength of a new implementation compiling, no new experiment
is claimed, and the manuscript is not claimed complete.

## 2. Before and after, at file level

Base `0a3cbd8`, head after six commits.

* tracked files: **714 → 746**
* **32 files added, 5 modified, 0 deleted, 0 renamed, 0 moved**
* one untracked file present throughout and deliberately not committed:
  `DefRec-Establishing Physical Function.pdf`, a reference copy

Added:

```
defense4/timing/TIMING_MODEL.md
defense4/timing/TIMEOUT_AND_RETRANSMISSION_AUDIT.md
defense4/timing/TIMING_ONLY_RERUN_PLAN.md
defense4/timing/active_harness/{README.md,dnp3_codec.py,session.py,frozen_builders.py,
                                read_driver.py,sbo_driver.py,tests/test_active_harness.py}
defense4/timing/audit_current/HISTORICAL_VS_CORRECTED.md
defense4/timing/audit_current/tools/{timeout_and_tcp_audit.py,make_model_figures.py,
                                     make_campaign_manifest.py}
defense4/timing/audit_current/outputs/{timeout_and_tcp_audit.json,
                                       CAMPAIGN_V1_CAPTURE_MANIFEST.csv,
                                       CAMPAIGN_V1_CAPTURE_MANIFEST.json}
defense4/timing/figures/model/{README.md, and the two figures' 7-file artefact sets}
```

Modified: `README.md`, `defense4/timing/README.md`,
`defense4/timing/CLAIMS_AND_LIMITATIONS.md`, `defense4/timing/BRANCH_MAP.md`,
`defense4/timing/figures/schematics/README.md`,
`defense4/timing/audit_current/CLAIM_EVIDENCE_MATRIX_CORRECTED.md`.

## 3. Nothing was moved or removed, and why

`CLEANUP_PLAN.md` was written for the 2026-08-24 prune and is labelled historical. This session
proposes **no** moves and **no** removals, so it adds nothing to that plan.

The reason is that the conditions the plan's classifications were for no longer hold. The tree
is already organised as `defense4/timing/` with the exact experiment source immutable, one
canonical evidence tree, and generated outputs rebuilt rather than tracked. The duplicate
`E_FINAL` copies the plan was written to resolve are not in the working tree at all: they were
pruned on 2026-08-24 and exist at the archive tag, where they are proven identical by tree
hash. There is no remaining duplicate, nothing whose only copy would be at risk, and no file
that would be better somewhere else.

New material was placed so that it does not disturb what is already verified: the corrected
harness in its own directory rather than beside the frozen one; the new tools and their outputs
under `audit_current/` rather than in the green reproduction, so `reproduce.sh` and its
publication gate were not perturbed; and the new figures in `figures/model/` rather than in
`paper/rewrite/figures/`, so no manuscript figure changed.

## 4. Commits

Six, in the order the work was done, each a single unit:

| commit | unit |
|---|---|
| `c3048b7` | timing model and the timeout audit, with the committed audit tool and its output |
| `931aa1c` | the corrected active harness and its 42 offline tests |
| `d56721e` | the two model diagrams and their generator |
| `bd242f1` | the rerun package and the sweep-composition correction |
| `0ad7fc5` | three stale descriptions corrected, and the historical-versus-corrected comparison |
| `<this one>` | the branch map rebuilt from verified state, and this record |

## 5. What a reader should not conclude from this session

* That any published number changed. None did.
* That the mechanism was newly validated on hardware. Nothing ran on hardware.
* That the timeout question is closed in every direction. The master side is settled with a
  measured margin; the relay's timers and its select window remain unrecorded.
* That the manuscript is revised. The section edits are a proposal, unapplied.
