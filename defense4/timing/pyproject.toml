[project]
name = "dnp3-timing-reproduction"
version = "1.0.0"
description = "Timing-only reproduction for the DNP3 in-network timing obfuscation evaluation (READ, SELECT phase of SBO, OPERATE)."
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.24,<3",
    "scipy>=1.10,<2",
    "scikit-learn>=1.3,<2",
    "matplotlib>=3.7,<4",
]

[tool.uv]
package = false
