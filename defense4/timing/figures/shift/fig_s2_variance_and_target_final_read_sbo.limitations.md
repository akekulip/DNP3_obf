rho below 1 shows reduced variability and rejects a constant translation as the explanation under comparable conditions. It does not establish independence from the native interval, does not bound leakage, and is not evidence that the queues behaved as designed. Operations and target settings are kept separate and are never pooled. For OPERATE the configured codebook is known but the realized per-transaction draw was never observed, so a per-J result exists only where the corpus fixed one J per capture. Nothing here is a physical-operation-time measurement.
