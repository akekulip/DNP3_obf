> **Historical.** First-pass campaign_v1 figure, superseded by `paper/rewrite/figures/ndss/`. Predates the read/control lane separation and the withdrawal of the jackknife and fold-bootstrap intervals. Do not quote as current.

### fig_c08_operating_envelope

**Measured effect of the two offsets.** (a) sweep of $D_A$ at $D_R=4$ ms; (b) fixed budget $D_A+D_R=24$ ms, split varied.

**Statistics.** One configuration point per marker, 200 to 400 READ transactions each; medians over the master-facing capture. The horizon H = 30.8 ms follows from the fail-open pass budget of the loaded program.
