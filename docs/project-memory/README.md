# Project memory (snapshot backup)

Read-only snapshot of the Claude Code project-memory notes for this repository,
copied here for backup. The live, authoritative copies live outside the repo in
`~/.claude/projects/-home-philip-Projects-DNP3/memory/` and are **not** auto-synced
with this folder — re-copy to refresh.

`MEMORY.md` is the index. Notes are durable facts/rules about the project (rig
topology, governing spec, prior results, working preferences). No credentials are
stored in these notes: the rig SSH/sudo password is deliberately kept out (notes use
a `<pw>` placeholder and state it is "not stored here").
