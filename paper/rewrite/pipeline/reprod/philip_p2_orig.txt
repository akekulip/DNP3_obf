Industrial control systems and grids run on protocols, and a device, its type, and model can be identified from the traffic leak and behavior. By learning which devices are communicating on the network and how they behave, because a control-related attack, such as a false-data injection attack on state estimation, only works if the attacker knows the target [3],[4]. In December 2025 remote intruders reached the Ukrainian power grid and left 225,000 users in the dark. Recent implementations like Ditto pad every packet and inject dummy packets so the size, timing, and so have their prior implementations sought to obfuscate the flow.
