# Fact-conflict report

Statements in the Claude-generated manuscript (`archive_claude_generated/`) that conflict with,
exceed, or are not traceable to the final code and evidence. Each row: the generated claim → the
verified reality (with source) → the required correction. Sources: `defense4_rrc_bor_unified12.p4`,
`defense4_rrc_bor_unified12_setup.py`, `E_FINAL/scripts/clrt_extract.py`, `E_FINAL/VERDICT.json`,
`CLAIM_EVIDENCE_LEDGER.md`. Verified 2026-08-14.

| # | Generated statement | Verified reality (source) | Correction |
|---|---|---|---|
| F1 | "CLRT is the interval between the transport-layer ACK and the application-layer response" (used consistently, but the term "ACK" is left ambiguous across the paper). | `clrt_extract.py:3-5,28-31`: **T_ack = first R2M packet with the TCP ACK flag set (`flags & 0x10`)** after the request; **T_resp = first R2M DNP3 application response (`func 0x81`, i.e. DNP3 app byte 12 = 0x81)**. CLRT = T_resp − T_ack. | Define ONCE: CLRT is between (i) the first outstation→master **TCP segment carrying the ACK flag** after the request and (ii) the first **DNP3 application-layer response (function 0x81)**. It is genuinely cross-layer (TCP transport vs DNP3 application). Never call it a "DNP3 link ACK" or a "DNP3 transport" event. |
| F2 | "features that survive the absence of payload decoding"; abstract "without decoding the payload." | Unencrypted DNP3 is fully parseable; `clrt_extract.py` and `size_reconstruct.py` in fact PARSE the DNP3 bytes (func codes, link CRC). The threat does not *require* payload decoding, but the payload is available. | Say the threat uses **metadata-only features (timing + segment sizes)** and does **not depend on application semantics**; do NOT imply the payload is unavailable on an unencrypted link. |
| F3 | "the master reassembles the identical response"; "byte for byte identical"; "the identical 49-byte response." | `VERDICT.json`: byte-identity to a source frame is NOT established (no paired source-side oracle). Evidence = sequence-contiguous, DNP3-block-CRC-valid, IP/TCP-checksum-valid 49-byte **reconstruction**. | Remove "identical response / byte-identical" from abstract, intro, body, captions. State only **CRC-valid, checksum-valid, sequence-contiguous 49-byte reconstruction**. (Ledger CL-7, CL-8.) |
| F4 | "no DNP3 byte is modified and no DNP3 CRC is recomputed" stated as a measured fact. | The P4 egress carve does not touch DNP3 bytes *by design* (only IP/TCP checksums recomputed), but the **measured** oracle only shows the reconstruction is CRC/checksum-valid. | Separate design intent ("the mechanism does not edit DNP3 bytes") from the **bounded measured claim** (CRC/checksum-valid reconstruction). Do not present byte-preservation as directly measured against a source oracle. |
| F5 | "its default action forwards the packet unmodified rather than dropping it." | `defense4_rrc_bor_unified12.p4:2318`: `const default_action = cmt_fwd()` ("fail-OPEN: outcome==0 is unreachable; forward, never drop"). **CONFIRMED.** But a stale comment at `:1174` reads "unset → tbl_commit default (drop)" — misleading; the actual default is `cmt_fwd`. | The statement is correct for the terminal default. Add the precision of F13 (many recognized outcomes explicitly drop). Optionally note the code comment at :1174 is stale. |
| F6 | "There is no configuration snapshot; rollback is a forward, readback-checked teardown to a plain forwarding state." | `defense4_rrc_bor_unified12_setup.py` (`hw_rollback`): no snapshot capture; rollback disables pktgen → shape off → timing off → delete PRE, each readback-checked. **CONFIRMED.** | Retain; it is accurate. (No change.) |
| F7 | "We assume no TCP timestamp option on the protected flow … the pipeline gates on this option and the evaluated captures carry none." | Two mechanisms exist: (a) the campaign **disabled** TCP timestamps on the host (`net.ipv4.tcp_timestamps=0`) and **verified absence at packet level** in preflight (`h3_operate_j6_single_result.md`); (b) the P4 parser carries `tcp_opt12` handling and the anti-subtraction claim is gated on the timestamp option reading 0. | State the **implemented+verified** behavior precisely: TCP timestamps were disabled on the host and confirmed absent at packet level in preflight; the pipeline additionally parses the timestamp option and the timing argument is asserted only when it is absent. Do not conflate host-disable with a pipeline drop. |
| F8 | "J is drawn … from a bounded codebook so that a passive observer cannot predict it" and "a secret internal delay." | `VERDICT.json` / codebook: J is a per-transaction PRNG draw range-matched into a control-plane codebook; **master-visible ACK/echo are J-independent** (measured). "Secret" is not established by the threat model (codebook exposure / observation model not analyzed for secrecy). | Do not call J "secret." State the **measured property**: the master-visible ACK-to-echo interval is **invariant to J** across the evaluated J values (2/6/12 ms). Secrecy of J is out of scope. (Ledger CL-11, CL-13.) |
| F9 | "released once at T0+J"; "exactly one … OPERATE" implying single relay delivery. | `VERDICT.json`: relay-facing T0+J and delivery multiplicity NOT captured (dp68 internal, no tap). Master issued one OPERATE per transaction (observed). | Never state or imply a measured single relay-facing release. Single release at T0+J is **model-verified, not observed**; the master-facing observation shows one OPERATE issued. (Ledger CL-13, CL-14.) |
| F10 | "hides operation timing" (control-command mode). | Measured property = a **J-invariant master-facing ACK-to-echo interval**; the relay's true actuation timing is not observed. | Narrow to: the mechanism **is designed to mitigate** the operation-timing fingerprint; the **measured** result is a J-invariant master-facing interval. (Ledger CL-11, L7.) |
| F11 | "because a DNP3 outstation segments deterministically, the shape repeats identically" (general). | Measured on **one SEL-751** and one eligible 49-byte class. | Scope every determinism/normalization claim to the evaluated device and response class; do not generalize to all DNP3 outstations. (Ledger CL-18.) |
| F12 | "Byte 28 is a selected existing DNP3 CRC-block boundary … TCP may segment a byte stream at any offset, so this is a design choice." | 49-byte layout = link header+CRC (10) + block0+CRC (18) + block1+CRC (18) + final block+CRC (3); cut at 28 = after header+block0. **CONFIRMED**; correctly framed as a design/verification choice. | Retain; it is accurate. Ensure the exact 49-byte layout is shown (figure) and the "design choice, not a TCP requirement" phrasing is kept. (Ledger CL-10.) |
| F13 | "the design never drops on the default path"; fail-open framed globally. | `tbl_commit` const map: recognized outcomes **explicitly drop** — `OUT_BADPORT`, `OUT_CLONE`, `OUT_PKTGEN_DROP`, `OUT_BLOCK_REJECT`, `OUT_RESP_DUP_SUPP`, and the blocker-terminate outcomes (`OUT_RB_*`, `OUT_AB_*`) → `cmt_drop`. Ineligible/unsupported/bypass → `cmt_fwd`. | Distinguish **explicit fail-open dispositions** for recognized unsupported/ineligible traffic (`cmt_fwd`) from **explicit drops** for internal-only packets (bad port, tokens, clones, duplicates, terminated blockers) and from the **terminal default** (`cmt_fwd`) for the unreachable unknown outcome. |
| F14 | "production-representative hardware." | The testbed is one physical Tofino-1 + one physical SEL-751; no deployment/production evidence. | Remove "production-representative / deployment-ready." Say **physical hardware** (a real Tofino-1 switch and a real SEL-751 relay). |
| F15 | Related Work placed AFTER Conclusion (structure). | NDSS convention + prompt §4.1: Conclusion must be the last argumentative section. | Move Related Work to before the Conclusion (after Introduction or before Evaluation); keep Conclusion last. |
| F16 | Abstract/intro use "primitive" for both mechanisms; the defense is unnamed but "response shaping"/"control-command mode" are our coinages. | Consistent with the unnamed-defense directive. | No conflict; ensure the running example teaches each mechanism before the coinage appears. |

## Cross-cutting
- **Layer/term consistency (F1):** fix the single most important recurring ambiguity — the exact two
  CLRT packets — and use one definition throughout.
- **Measured vs modeled vs unobserved (F3,F4,F8,F9,F10):** the manuscript repeatedly blends the three.
  Every timing/segmentation/BOR sentence must carry the correct evidence status, matching the figure
  tri-state legend (measured / modeled / unobserved).
- **Generality (F11,F14):** scope to one device, one response class, physical hardware.
